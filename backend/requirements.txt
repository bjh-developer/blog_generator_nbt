fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0
trafilatura>=1.8
# discovery+scrape via Firecrawl v2 /search (plain httpx, no SDK needed)
# optional semantic verify (USE_EMBEDDINGS=1)
# sentence-transformers>=2.7
# dev
pytest>=8.0
pytest-asyncio>=0.23
